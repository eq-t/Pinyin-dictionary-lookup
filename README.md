# Pinyin-dictionary-lookup
Given a pinyin dictionary with a size of millions of entries, the use case is to implement a lookup function that takes a pinyin string as input and outputs the corresponding Chinese characters in a CSV file. Analyze the program's memory usage and lookup time (the program's memory usage must not exceed 5MB).
